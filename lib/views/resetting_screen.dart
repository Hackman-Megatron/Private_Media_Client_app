import 'package:flutter/material.dart';

class ResettingScreen extends StatelessWidget {
  const ResettingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text(
            "Resetting Password"
        ),
      ),
    );
  }
}
